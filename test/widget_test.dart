// Basic smoke test placeholder — update when integration tests are added.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Firebase requires a real device/emulator to initialise.
    // Add integration tests using package:integration_test for full coverage.
    expect(true, isTrue);
  });
}
